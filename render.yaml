services:
  - type: web
    name: ai-xiaoguan
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist/client
    autoDeploy: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
